{
  "name": "plantilladasvercel",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "chmod +x node_modules/.bin/vite || true",
    "build": "vite build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "vercel": "^39.2.2"
  },
  "devDependencies": {
    "vite": "^3.0.0"
  }
}
